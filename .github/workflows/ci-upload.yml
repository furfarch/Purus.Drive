name: CI Build & Upload to TestFlight (DISABLED)

# This workflow has been disabled to prevent automated uploads to App Store / TestFlight.
# It is intentionally reduced to a no-op checkout step so it does not use secrets or run fastlane.

on:
  workflow_dispatch: {}

jobs:
  disabled:
    name: Disabled job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (no-op)
        uses: actions/checkout@v4
      - name: Disabled notice
        run: |
          echo "CI upload workflow has been disabled. To re-enable, restore the original workflow file."
